if [ -f ~/.bashrc ]; then . ~/.bashrc; fi

# general
alias ll="ls -la"
alias clera="clear"
alias vi=vim
alias clipboard="pbcopy"

export CLICOLOR=1
export export GPG_TTY=$(tty)

# git
alias pull="git pull"
alias push="git push"
alias commit="git commit"

