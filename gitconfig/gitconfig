[user]
    useConfigOnly = true
    name = Jeremy Stucki

[include]
    path = ~/.machine-specific-gitconfig

[commit]
    gpgsign = true

[core]
	editor = vim
    excludesfile = ~/.gitignore

[diff]
	algorithm = patience

[merge]
	ff = no

[push]
	default = upstream

[rerere]
	enabled = true

[alias]
	ignore = "!gi() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; gi"
